# For Vercel Postgres (optional - if you want persistent storage)
# You'll get these from Vercel dashboard after adding Postgres storage
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# The app works without these - it will use in-memory storage
# which resets on each deployment but is perfect for demo purposes